<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Myary.Views.FavoritesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Myary.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#FFF8F0"
    Loaded="Page_Loaded">

    <Grid Padding="40,30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Your Favorites" FontSize="28" 
                   FontWeight="Bold" Foreground="#363537" Margin="0,0,0,25"/>

        <ListView x:Name="FavoritesList" Grid.Row="1"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" CornerRadius="10" Padding="20,15"
                            Margin="0,0,0,10" BorderBrush="#E7C6C9" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Spacing="4">
                                <TextBlock Text="{Binding Title}" FontSize="18" 
                                           FontWeight="SemiBold" Foreground="#363537"/>
                                <TextBlock Text="{Binding Date}"
                                           FontSize="13" Foreground="#999"/>
                            </StackPanel>

                            <Button Grid.Column="1" Content="Open" VerticalAlignment="Center"
                                    Background="#D6A2A8" Foreground="#363537"
                                    CornerRadius="6" Padding="15,8" Click="OpenFavorite_Click"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel x:Name="EmptyState" Grid.Row="1" Visibility="Collapsed"
                    VerticalAlignment="Center" HorizontalAlignment="Center">
            <FontIcon Glyph="&#xEB51;" FontSize="48" Foreground="#E7C6C9"/>
            <TextBlock Text="No favorites yet!" FontSize="18" Foreground="#999"
                       HorizontalAlignment="Center" Margin="0,15,0,0"/>
            <TextBlock Text="Tap the heart icon to make your favorites."
                       FontSize="14" Foreground="#BBB" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>
